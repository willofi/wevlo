services:
  api:
    image: ${IMAGE_REF}
    container_name: wevlo-api
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "4000:4000"
    volumes:
      - /opt/wevlo/data/attachments:/opt/wevlo/data/attachments
